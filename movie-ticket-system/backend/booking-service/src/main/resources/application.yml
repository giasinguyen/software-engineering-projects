server:
  port: 8083

spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:h2:mem:bookingdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
  rabbitmq:
    host: armadillo.rmq.cloudamqp.com
    port: 5671
    username: cucfpjwl
    password: Jbwwm-HQWFMdIqi2pExZyZOjmi1AnF2D
    virtual-host: cucfpjwl
    ssl:
      enabled: true

app:
  rabbitmq:
    exchange: movie.ticket.exchange
    routing-key:
      booking-created: booking.created
    queue:
      booking-update: booking.update.queue
    binding:
      payment-completed: payment.completed
      booking-failed: booking.failed
