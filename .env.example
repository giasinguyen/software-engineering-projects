# ─── Docker Compose Environment Variables ───
# Copy this file to .env and fill in values before running docker-compose up

# MongoDB (used by docker-compose for local MongoDB container)
# No auth needed for local dev — services connect via mongodb://mongo:27017/minifood
